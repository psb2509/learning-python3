import exp15_modules;
import exp14_dictionary;
import random;

exp15_modules.family();         # The right way to use a module is use the module name and then use the function from the module .
print('-------');
print(exp14_dictionary.family);
print('-------');

x = random.randint(1,31);
print(x);